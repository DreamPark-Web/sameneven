export default function InsightPage() {
  return (
    <main className="min-h-screen bg-[#141414] flex items-center justify-center">
      <p className="text-white">Insight komt hier.</p>
    </main>
  )
}
```

Sla op met `Ctrl + S`. Dan in de terminal:
```
git add .
git commit -m "fix: lege insight pagina toegevoegd"
git push